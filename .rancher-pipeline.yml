stages:
- name: build image
  steps:
  - publishImageConfig:
      dockerfilePath: ./Dockerfile
      buildContext: .
      tag: flask_hello:v1
      pushRemote: true
      registry: index.docker.io
timeout: 60
notification: {}
